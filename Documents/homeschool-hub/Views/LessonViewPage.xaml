<Page x:Class="HomeschoolHub.Views.LessonViewPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#4A90E2" Padding="15">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BackButton"
                        Click="BackButton_Click"
                        Background="Transparent"
                        Foreground="White"
                        Content="← Back"
                        FontSize="16"
                        Margin="0,0,20,0"/>
                <TextBlock x:Name="CategoryTitle" 
                          Text="Lessons" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Lesson List -->
        <ScrollViewer Grid.Row="1" Padding="20">
            <StackPanel x:Name="LessonsStackPanel"/>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="15">
            <TextBlock Text="Complete lessons to track your progress!" 
                      FontSize="14" 
                      HorizontalAlignment="Center"
                      Foreground="Gray"/>
        </Border>
    </Grid>
</Page>

