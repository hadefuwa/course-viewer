---
import Base from "../layouts/Base.astro";
import Section from "../components/Section.astro";
import { getCourse } from "../lib/supabase";

const course = await getCourse();
const teacher = course?.sections?.find((s: any) => s.kind === "teacher");
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---
<Base title="Teacher's notes">
  <section class="container">
    <p style="margin: 18px 0 0;">
      <a href={`${base}/unit6`}>← Back to course overview</a>
    </p>
    <div class="ws-hero">
      <div>
        <div class="label">Lecturer reference</div>
        <h1>{teacher?.title || "Teacher's notes"}</h1>
      </div>
    </div>
    {teacher?.sections.map((s) => <Section section={s} />)}
  </section>
</Base>
